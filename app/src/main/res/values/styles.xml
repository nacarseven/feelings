<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/windowsBlueTwo</item>
        <item name="colorPrimaryDark">@color/midBlue</item>
        <item name="colorAccent">@color/grey</item>
    </style>

    <style name="HintSearchEditText">
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/grey</item>
    </style>

    <style name="CircleButton">
        <item name="android:clickable">true</item>
        <item name="android:focusable">true</item>
        <item name="android:backgroundTint">@color/color_search_button</item>
        <item name="borderWidth">0dp</item>
        <item name="rippleColor">@color/lightBlue</item>
    </style>

</resources>
